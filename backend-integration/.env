# AFZ Backend Configuration - Development Environment
# This is for local testing only

# Server Configuration
PORT=3000
NODE_ENV=development
ALLOWED_ORIGINS=http://localhost:8080,http://127.0.0.1:8080,http://localhost:3000

# Database Configuration (SQLite for development - no setup required)
# We'll use in-memory storage for now to avoid database setup complexity
DB_HOST=localhost
DB_PORT=5432
DB_NAME=afz_website_dev
DB_USER=dev_user
DB_PASSWORD=dev_password

# Email Configuration - Gmail Production Setup
EMAIL_SERVICE=gmail
# Your AFZ Gmail address
EMAIL_USER=afz.zambia@gmail.com
# Replace with your 16-character app password from Gmail (no spaces)
EMAIL_PASSWORD=dmyrhrvzamluwabn
EMAIL_FROM=noreply@afz.org.zm
EMAIL_ADMIN=afz.zambia@gmail.com

# Security - Development Keys
JWT_SECRET=development-jwt-secret-key-not-for-production
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX=100

# Development Settings
LOG_LEVEL=debug
LOG_FILE=./logs/afz-backend-dev.log

# CORS Configuration - Permissive for development
CORS_ORIGINS=http://localhost:8080,http://127.0.0.1:8080,http://localhost:3000
CORS_CREDENTIALS=true

# Health Check
HEALTH_CHECK_TOKEN=dev-health-check-token

# Session Configuration
SESSION_SECRET=development-session-secret
SESSION_TIMEOUT=86400000